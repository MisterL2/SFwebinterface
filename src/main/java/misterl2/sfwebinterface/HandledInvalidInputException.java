package misterl2.sfwebinterface;

public class HandledInvalidInputException extends Exception { //When the exception has already been dealt with, but a notifier must still be sent in order to avoid returning a misleading null value
}
